import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // Turbopack Einstellungen gehören hier nicht mehr rein, 
    // das erkennt Next.js 16 automatisch.
};

export default nextConfig;