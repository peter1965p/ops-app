import type { IconName } from "./Icon.Types";

export interface CommandItem {
    id: string;
    label: string;
    icon: IconName;
    action: () => void;
}

export interface CommandItem {
    id: string;
    label: string;
    icon: IconName;
    action: () => void;
}
